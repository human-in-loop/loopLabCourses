import { useState } from "react";
import { Link } from "wouter";
import { useQuery } from "@tanstack/react-query";
import { motion } from "framer-motion";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { Button } from "@/components/ui/button";
import { Badge } from "@/components/ui/badge";

interface FeaturedContent {
  recentPosts: Array<{
    id: string;
    title: string;
    excerpt: string;
    publishedAt: string;
    slug: string;
  }>;
  latestPodcast: {
    id: string;
    title: string;
    description: string;
    embedUrl?: string | null;
    audioUrl?: string | null;
    publishedAt: string;
  } | null;
  popularCourses: Array<{
    id: string;
    title: string;
    description: string;
    thumbnail: string;
    category: string;
    isPremium: boolean;
    enrollmentCount: number;
  }>;
}

export default function Home() {
  const { data: featured, isLoading } = useQuery<FeaturedContent>({
    queryKey: ["/api/featured-content"],
    queryFn: async () => {
      const response = await fetch("/api/featured-content");
      if (!response.ok) throw new Error("Failed to fetch featured content");
      return response.json();
    },
  });

  return (
    <div className="min-h-screen bg-loop-dark text-white">
      {/* Hero Section */}
      <section className="relative pt-20 pb-16 px-4 overflow-hidden">
        <div className="absolute inset-0 bg-gradient-to-br from-loop-purple/20 via-transparent to-loop-accent/20"></div>
        <div className="max-w-6xl mx-auto relative z-10">
          <motion.div
            initial={{ opacity: 0, y: 30 }}
            animate={{ opacity: 1, y: 0 }}
            transition={{ duration: 0.8 }}
            className="text-center"
          >
            <h1 className="text-5xl md:text-7xl font-bold mb-6">
              <span className="gradient-text">Human In Loop</span>
            </h1>
            <p className="text-xl md:text-2xl text-gray-300 mb-8 max-w-3xl mx-auto">
              Mastering the Intersection of Humans and AI
            </p>
            <p className="text-lg text-gray-400 mb-12 max-w-2xl mx-auto">
              A digital hub for AI education, insights, and consulting.
              Where human intelligence meets artificial intelligence to create extraordinary outcomes.
            </p>
            <div className="flex flex-col sm:flex-row gap-4 justify-center">
              <Link href="/courses">
                <Button size="lg" className="bg-loop-purple hover:bg-loop-purple/80 text-lg px-8 py-3">
                  Explore Courses
                </Button>
              </Link>
              <Link href="/services">
                <Button size="lg" variant="outline" className="border-loop-accent text-loop-accent hover:bg-loop-accent hover:text-white text-lg px-8 py-3">
                  Get Consulting
                </Button>
              </Link>
            </div>
          </motion.div>
        </div>
      </section>

      {/* What We Offer Section */}
      <section className="py-16 px-4 bg-gray-900/50">
        <div className="max-w-6xl mx-auto">
          <motion.div
            initial={{ opacity: 0, y: 20 }}
            whileInView={{ opacity: 1, y: 0 }}
            transition={{ duration: 0.6 }}
            viewport={{ once: true }}
          >
            <h2 className="text-4xl font-bold text-center mb-4">
              <span className="gradient-text">What We Offer</span>
            </h2>
            <p className="text-gray-400 text-center mb-12 max-w-2xl mx-auto">
              Comprehensive resources to help you navigate the AI revolution
            </p>

            <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-8">
              {/* Courses Card */}
              <Link href="/courses">
                <Card className="bg-gradient-to-br from-blue-500/10 to-blue-600/10 border-blue-500/20 hover:border-blue-500/40 transition-all duration-300 cursor-pointer h-full">
                  <CardContent className="p-6 text-center">
                    <div className="bg-blue-500/20 p-4 rounded-full mx-auto mb-4 w-fit">
                      <i className="fas fa-graduation-cap text-blue-400 text-2xl"></i>
                    </div>
                    <h3 className="text-xl font-semibold text-white mb-3">Loop Lab Courses</h3>
                    <p className="text-gray-400 text-sm">
                      Hands-on courses on AI, machine learning, and human-AI collaboration
                    </p>
                  </CardContent>
                </Card>
              </Link>

              {/* Podcast Card */}
              <Link href="/podcast">
                <Card className="bg-gradient-to-br from-purple-500/10 to-purple-600/10 border-purple-500/20 hover:border-purple-500/40 transition-all duration-300 cursor-pointer h-full">
                  <CardContent className="p-6 text-center">
                    <div className="bg-purple-500/20 p-4 rounded-full mx-auto mb-4 w-fit">
                      <i className="fas fa-microphone text-purple-400 text-2xl"></i>
                    </div>
                    <h3 className="text-xl font-semibold text-white mb-3">AI Insights Podcast</h3>
                    <p className="text-gray-400 text-sm">
                      Conversations with AI leaders, researchers, and practitioners
                    </p>
                  </CardContent>
                </Card>
              </Link>

              {/* Blog Card */}
              <Link href="/blog">
                <Card className="bg-gradient-to-br from-green-500/10 to-green-600/10 border-green-500/20 hover:border-green-500/40 transition-all duration-300 cursor-pointer h-full">
                  <CardContent className="p-6 text-center">
                    <div className="bg-green-500/20 p-4 rounded-full mx-auto mb-4 w-fit">
                      <i className="fas fa-pen-fancy text-green-400 text-2xl"></i>
                    </div>
                    <h3 className="text-xl font-semibold text-white mb-3">Industry Insights</h3>
                    <p className="text-gray-400 text-sm">
                      Articles, case studies, and deep dives into AI trends and applications
                    </p>
                  </CardContent>
                </Card>
              </Link>

              {/* Services Card */}
              <Link href="/services">
                <Card className="bg-gradient-to-br from-orange-500/10 to-orange-600/10 border-orange-500/20 hover:border-orange-500/40 transition-all duration-300 cursor-pointer h-full">
                  <CardContent className="p-6 text-center">
                    <div className="bg-orange-500/20 p-4 rounded-full mx-auto mb-4 w-fit">
                      <i className="fas fa-handshake text-orange-400 text-2xl"></i>
                    </div>
                    <h3 className="text-xl font-semibold text-white mb-3">AI Consulting</h3>
                    <p className="text-gray-400 text-sm">
                      Strategic guidance for AI integration and human-AI collaboration
                    </p>
                  </CardContent>
                </Card>
              </Link>
            </div>
          </motion.div>
        </div>
      </section>

      {/* Featured Content Section */}
      <section className="py-16 px-4">
        <div className="max-w-6xl mx-auto">
          <motion.div
            initial={{ opacity: 0, y: 20 }}
            whileInView={{ opacity: 1, y: 0 }}
            transition={{ duration: 0.6 }}
            viewport={{ once: true }}
          >
            <h2 className="text-4xl font-bold text-center mb-12">
              <span className="gradient-text">Featured Content</span>
            </h2>

            <div className="grid grid-cols-1 lg:grid-cols-3 gap-8">
              {/* Latest Podcast */}
              <div className="lg:col-span-1">
                <h3 className="text-2xl font-semibold mb-6 flex items-center">
                  <i className="fas fa-microphone text-purple-400 mr-3"></i>
                  Latest Podcast
                </h3>

                {featured?.latestPodcast ? (
                  <Card className="bg-gray-800/50 border-gray-700">
                    <CardContent className="p-6">
                      <h4 className="text-lg font-semibold text-white mb-2">
                        {featured.latestPodcast.title}
                      </h4>
                      <p className="text-gray-400 text-sm mb-4">
                        {featured.latestPodcast.description}
                      </p>

                      {/* Embed Spotify if available; otherwise fall back to audio */}
                      {featured.latestPodcast.embedUrl ? (
                        <div className="mb-4">
                          <iframe
                            src={featured.latestPodcast.embedUrl}
                            title={`Spotify player: ${featured.latestPodcast.title}`}
                            width="100%"
                            height="152"
                            frameBorder={0}
                            allow="autoplay; clipboard-write; encrypted-media; fullscreen; picture-in-picture"
                            loading="lazy"
                          />
                        </div>
                      ) : featured.latestPodcast.audioUrl ? (
                        <audio controls preload="none" className="w-full mb-4">
                          <source src={featured.latestPodcast.audioUrl} type="audio/mpeg" />
                          Your browser does not support the audio element.
                        </audio>
                      ) : null}

                      <Link href="/podcast">
                        <Button
                          variant="outline"
                          className="w-full border-purple-500/30 text-purple-400 hover:bg-purple-500/10"
                        >
                          View All Episodes
                        </Button>
                      </Link>
                    </CardContent>
                  </Card>
                ) : (
                  <Card className="bg-gray-800/50 border-gray-700">
                    <CardContent className="p-6 text-center">
                      <p className="text-gray-400">Coming soon...</p>
                    </CardContent>
                  </Card>
                )}
              </div>

              {/* Recent Blog Posts (unchanged) */}
              <div className="lg:col-span-2">
                <h3 className="text-2xl font-semibold mb-6 flex items-center">
                  <i className="fas fa-newspaper text-green-400 mr-3"></i>
                  Recent Articles
                </h3>
                {featured?.recentPosts?.length ? (
                  <div className="space-y-4">
                    {featured.recentPosts.slice(0, 3).map((post) => (
                      <Link key={post.id} href={`/blog/${post.slug}`}>
                        <Card className="bg-gray-800/50 border-gray-700 hover:border-green-500/30 transition-colors cursor-pointer">
                          <CardContent className="p-4">
                            <div className="flex justify-between items-start mb-2">
                              <h4 className="text-lg font-semibold text-white hover:text-green-400 transition-colors">
                                {post.title}
                              </h4>
                              <span className="text-xs text-gray-500 ml-4">
                                {new Date(post.publishedAt).toLocaleDateString()}
                              </span>
                            </div>
                            <p className="text-gray-400 text-sm">{post.excerpt}</p>
                          </CardContent>
                        </Card>
                      </Link>
                    ))}
                    <Link href="/blog">
                      <Button variant="outline" className="w-full border-green-500/30 text-green-400 hover:bg-green-500/10">
                        View All Articles
                      </Button>
                    </Link>
                  </div>
                ) : (
                  <Card className="bg-gray-800/50 border-gray-700">
                    <CardContent className="p-6 text-center">
                      <p className="text-gray-400">Articles coming soon...</p>
                    </CardContent>
                  </Card>
                )}
              </div>
            </div>


            {/* Popular Courses */}
            <div className="mt-12">
              <h3 className="text-2xl font-semibold mb-6 flex items-center">
                <i className="fas fa-star text-blue-400 mr-3"></i>
                Popular Courses
              </h3>
              {featured?.popularCourses?.length ? (
                <div className="grid grid-cols-1 md:grid-cols-2 gap-6">
                  {featured.popularCourses.slice(0, 2).map((course) => (
                    <Link key={course.id} href={`/courses/${course.id}`}>
                      <Card className="bg-gray-800/50 border-gray-700 hover:border-blue-500/30 transition-colors cursor-pointer">
                        <CardContent className="p-6">
                          <div className="flex items-start space-x-4">
                            {course.thumbnail && (
                              <img
                                src={course.thumbnail}
                                alt={course.title}
                                className="w-16 h-16 rounded-lg object-cover"
                              />
                            )}
                            <div className="flex-1 min-w-0">
                              <div className="flex items-center justify-between mb-2">
                                <h4 className="text-lg font-semibold text-white truncate">
                                  {course.title}
                                </h4>
                                {course.isPremium && (
                                  <Badge className="bg-yellow-500/20 text-yellow-400 ml-2">
                                    Premium
                                  </Badge>
                                )}
                              </div>
                              <p className="text-gray-400 text-sm mb-2">{course.description}</p>
                              <div className="flex items-center justify-between">
                                <Badge variant="outline" className="text-xs">
                                  {course.category}
                                </Badge>
                                <span className="text-xs text-gray-500">
                                  {course.enrollmentCount} enrolled
                                </span>
                              </div>
                            </div>
                          </div>
                        </CardContent>
                      </Card>
                    </Link>
                  ))}
                </div>
              ) : (
                <Card className="bg-gray-800/50 border-gray-700">
                  <CardContent className="p-6 text-center">
                    <p className="text-gray-400">Courses coming soon...</p>
                  </CardContent>
                </Card>
              )}
              <div className="text-center mt-6">
                <Link href="/courses">
                  <Button className="bg-blue-500 hover:bg-blue-600">
                    View All Courses
                  </Button>
                </Link>
              </div>
            </div>
          </motion.div>
        </div>
      </section>

      {/* CTA Section */}
      <section className="py-16 px-4 bg-gradient-to-br from-loop-purple/20 via-transparent to-loop-accent/20">
        <div className="max-w-4xl mx-auto text-center">
          <motion.div
            initial={{ opacity: 0, y: 20 }}
            whileInView={{ opacity: 1, y: 0 }}
            transition={{ duration: 0.6 }}
            viewport={{ once: true }}
          >
            <h2 className="text-4xl font-bold mb-6">
              <span className="gradient-text">Ready to Bridge the AI Gap?</span>
            </h2>
            <p className="text-xl text-gray-300 mb-8 max-w-2xl mx-auto">
              Join thousands of professionals who are mastering the art of human-AI collaboration.
            </p>
            <div className="flex flex-col sm:flex-row gap-4 justify-center">
              <Link href="/courses">
                <Button size="lg" className="bg-loop-purple hover:bg-loop-purple/80 text-lg px-8 py-3">
                  Start Learning Today
                </Button>
              </Link>
              <Link href="/services">
                <Button size="lg" variant="outline" className="border-loop-accent text-loop-accent hover:bg-loop-accent hover:text-white text-lg px-8 py-3">
                  Book a Consultation
                </Button>
              </Link>
            </div>
          </motion.div>
        </div>
      </section>
    </div>
  );
}
